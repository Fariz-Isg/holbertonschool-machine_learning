# Regularization

This project explores various regularization techniques used in Deep Learning to prevent overfitting.

## Tasks

### 0. L2 Regularization Cost
Calculates the cost of a neural network with L2 regularization using the Frobenius norm of the weight matrices.
- Prototype: `def l2_reg_cost(cost, lambtha, weights, L, m):`
- Returns: the cost of the network accounting for L2 regularization.